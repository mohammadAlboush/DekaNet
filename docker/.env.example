# ============================================================================
# DOCKER ENVIRONMENT VARIABLES TEMPLATE - DigiDekan Production
# ============================================================================
# WICHTIG:
# 1. Kopiere diese Datei nach .env
# 2. Fülle alle Werte aus
# 3. Die .env Datei ist in .gitignore und wird NIEMALS committed!
# ============================================================================

# ============================================================================
# DATABASE CREDENTIALS
# ============================================================================
DB_USER=dekanat_user
DB_PASSWORD=GENERATE_SECURE_PASSWORD_HERE
DB_NAME=dekanat_production

# Generiere sicheres Passwort mit:
# python -c "import secrets; print(secrets.token_urlsafe(32))"

# ============================================================================
# BACKEND SECURITY KEYS
# ============================================================================
# KRITISCH: Diese Keys MÜSSEN einzigartig sein!
# Nutze die gleichen Keys wie in backend/.env.production

SECRET_KEY=GENERATE_64_CHAR_HEX_HERE
JWT_SECRET_KEY=GENERATE_64_CHAR_HEX_HERE

# Generiere Keys mit:
# python -c "import secrets; print(secrets.token_hex(32))"

# ============================================================================
# CORS CONFIGURATION
# ============================================================================
# Komma-separierte Liste erlaubter Origins
# WICHTIG: Ändere auf deine echte Production-Domain!

CORS_ORIGINS=https://your-domain.com,https://www.your-domain.com

# Beispiele:
# CORS_ORIGINS=https://digidekan.uni-beispiel.de
# CORS_ORIGINS=https://dekanat.example.com,https://www.dekanat.example.com

# ============================================================================
# LOGGING
# ============================================================================
LOG_LEVEL=WARNING

# Optionen: DEBUG, INFO, WARNING, ERROR, CRITICAL
# Production: WARNING oder ERROR
# Development: DEBUG oder INFO

# ============================================================================
# QUICK SETUP COMMANDS
# ============================================================================
#
# 1. Generiere alle Secrets auf einmal:
#    python -c "
#    import secrets
#    print('DB_PASSWORD=' + secrets.token_urlsafe(32))
#    print('SECRET_KEY=' + secrets.token_hex(32))
#    print('JWT_SECRET_KEY=' + secrets.token_hex(32))
#    "
#
# 2. Erstelle .env Datei:
#    cp .env.example .env
#    nano .env  # oder vim, code, etc.
#
# 3. Fülle ALLE Werte aus (siehe oben generierte Secrets)
#
# 4. WICHTIG: Nutze gleiche SECRET_KEY & JWT_SECRET_KEY wie in backend/.env.production!
#
# ============================================================================
